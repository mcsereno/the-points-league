"use client";
import { FormEvent,useState } from "react";
type Mode="signIn"|"register";
export function LoginForm({returnTo}:{returnTo:string}){const [mode,setMode]=useState<Mode>("signIn");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [username,setUsername]=useState("");const [confirmPassword,setConfirmPassword]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);const request=async(path:string,payload:Record<string,string>)=>{const r=await fetch(path,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const body=await r.json() as {error?:string;redirectTo?:string};if(!r.ok)throw new Error(body.error??"Something went wrong.");return body;};const signIn=async(event:FormEvent)=>{event.preventDefault();setLoading(true);setMessage("");try{const body=await request("/api/auth/login",{email,password,returnTo});if(!body.redirectTo)throw new Error("We could not sign you in.");window.location.assign(body.redirectTo);}catch(error){setMessage(error instanceof Error?error.message:"We could not sign you in.");setLoading(false);}};const register=async(event:FormEvent)=>{event.preventDefault();if(password!==confirmPassword){setMessage("Passwords do not match.");return;}setLoading(true);setMessage("");try{await request("/api/auth/register",{email,password,username});setMessage("Check your email to confirm your account, then sign in.");setMode("signIn");setPassword("");setConfirmPassword("");}catch(error){setMessage(error instanceof Error?error.message:"We could not create that account.");}finally{setLoading(false);}};const resend=async()=>{setLoading(true);setMessage("");try{await request("/api/auth/resend-confirmation",{email});setMessage("If that account needs confirmation, we sent another email.");}catch{setMessage("We could not send another confirmation email.");}finally{setLoading(false);}};return <><p className="eyebrow">MEMBER ACCESS</p><h1>{mode==="signIn"?"Sign in to the pool.":"Create your account."}</h1><p className="auth-copy">{mode==="signIn"?"Use the email and password you registered with.":"Confirm your email before the commissioner can approve your entry."}</p><div className="auth-tabs"><button type="button" className={mode==="signIn"?"active":""} onClick={()=>{setMode("signIn");setMessage("");}}>Sign in</button><button type="button" className={mode==="register"?"active":""} onClick={()=>{setMode("register");setMessage("");}}>Register</button></div>{mode==="signIn"?<form className="auth-form" onSubmit={signIn}><label><span>Email address</span><input autoComplete="email" required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label><span>Password</span><input autoComplete="current-password" required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button button-primary" disabled={loading}>{loading?"Signing in":"Sign in"}</button><button className="auth-back" type="button" disabled={loading||!email} onClick={resend}>Resend confirmation email</button></form>:<form className="auth-form" onSubmit={register}><label><span>Username</span><input autoComplete="username" maxLength={30} required value={username} onChange={e=>setUsername(e.target.value)}/></label><label><span>Email address</span><input autoComplete="email" required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label><span>Password <small>10 characters minimum</small></span><input autoComplete="new-password" minLength={10} required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label><span>Confirm password</span><input autoComplete="new-password" minLength={10} required type="password" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)}/></label><button className="button button-primary" disabled={loading}>{loading?"Creating account":"Create account"}</button></form>}{message&&<p className="auth-message" role="status">{message}</p>}</>}
