"use client";
import { FormEvent,useState } from "react";
export function LoginForm({returnTo}:{returnTo:string}){const [step,setStep]=useState<"email"|"code">("email");const [email,setEmail]=useState("");const [displayName,setDisplayName]=useState("");const [code,setCode]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);const requestCode=async(event:FormEvent)=>{event.preventDefault();setLoading(true);setMessage("");try{const r=await fetch("/api/auth/request-code",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,displayName})});const b=await r.json() as {error?:string};if(!r.ok)throw new Error(b.error??"The code could not be sent.");setStep("code");setMessage(`We sent a six-digit code to ${email.trim().toLowerCase()}.`);}catch(error){setMessage(error instanceof Error?error.message:"The code could not be sent.");}finally{setLoading(false);}};const verify=async(event:FormEvent)=>{event.preventDefault();setLoading(true);setMessage("");try{const r=await fetch("/api/auth/verify-code",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,code,returnTo})});const b=await r.json() as {error?:string;redirectTo?:string};if(!r.ok||!b.redirectTo)throw new Error(b.error??"That code could not be verified.");window.location.assign(b.redirectTo);}catch(error){setMessage(error instanceof Error?error.message:"That code could not be verified.");setLoading(false);}};return <><p className="eyebrow">MEMBER SIGN IN</p><h1>{step==="email"?"Enter the pool.":"Check your email."}</h1><p className="auth-copy">{step==="email"?"Enter your email and we'll send a one-time sign-in code. New players are added to the commissioner approval list.":"Enter the six-digit code from your email."}</p>{step==="email"?<form className="auth-form" onSubmit={requestCode}><label><span>Email address</span><input autoComplete="email" required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label><span>Name <small>New players only</small></span><input autoComplete="name" maxLength={80} value={displayName} onChange={e=>setDisplayName(e.target.value)}/></label><button className="button button-primary" disabled={loading}>{loading?"Sending code":"Email me a code"}</button></form>:<form className="auth-form" onSubmit={verify}><label><span>Six-digit code</span><input autoComplete="one-time-code" inputMode="numeric" maxLength={6} pattern="[0-9]{6}" required value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,"").slice(0,6))}/></label><button className="button button-primary" disabled={loading||code.length!==6}>{loading?"Signing in":"Verify and sign in"}</button><button className="auth-back" type="button" onClick={()=>{setStep("email");setCode("");setMessage("");}}>Use a different email</button></form>}{message&&<p className="auth-message" role="status">{message}</p>}</>}
