import { isSameOrigin,registerWithPassword } from "../../../lib/auth";
const emailPattern=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;const usernamePattern=/^[A-Za-z0-9][A-Za-z0-9 _-]{2,29}$/;
export async function POST(request:Request){if(!isSameOrigin(request))return Response.json({error:"Invalid request origin."},{status:403});const body=await request.json() as {email?:string;password?:string;username?:string};const email=body.email?.trim().toLowerCase()??"";const password=body.password??"";const username=body.username?.trim()??"";if(!emailPattern.test(email)||email.length>254)return Response.json({error:"Enter a valid email address."},{status:400});if(!usernamePattern.test(username))return Response.json({error:"Username must be 3 to 30 characters and use letters, numbers, spaces, hyphens, or underscores."},{status:400});if(password.length<10)return Response.json({error:"Use a password with at least 10 characters."},{status:400});try{await registerWithPassword(email,password,username);return Response.json({ok:true});}catch(error){return Response.json({error:error instanceof Error?error.message:"We could not create that account."},{status:400});}}
