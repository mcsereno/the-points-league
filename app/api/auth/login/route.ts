import { createMemberSession,isSameOrigin,safeReturnTo,sessionCookie,signInWithPassword } from "../../../lib/auth";
import { createOrUpdateMember } from "../../../lib/member-registration";
import { pendingEmail,sendMemberEmailOnce } from "../../../lib/email";
export async function POST(request:Request){if(!isSameOrigin(request))return Response.json({error:"Invalid request origin."},{status:403});const body=await request.json() as {email?:string;password?:string;returnTo?:string};const email=body.email?.trim().toLowerCase()??"";const password=body.password??"";if(!email||!password)return Response.json({error:"Enter your email and password."},{status:400});try{const user=await signInWithPassword(email,password);const {member,isNew}=await createOrUpdateMember(user);if(isNew&&member.status==="pending"){const message=pendingEmail(member.displayName);await sendMemberEmailOnce(`member:${member.id}:pending`,{to:user.email,...message});}const token=await createMemberSession(member.id);return Response.json({ok:true,redirectTo:safeReturnTo(body.returnTo??"/portal")},{headers:{"set-cookie":sessionCookie(token,request.url),"cache-control":"no-store"}});}catch(error){return Response.json({error:error instanceof Error?error.message:"We could not sign you in."},{status:400});}}
